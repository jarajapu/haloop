package org.apache.hadoop.mapred.iterative;

public interface LoopReduceOutputCacheFilter {

	/**
	 * decide whether the reduce output key,value needs to be cached
	 * @param key
	 * @param value
	 * @return
	 */
	public boolean isCache(Object key, Object value);
}
